# گزارش نهایی پروژه FileTransferApp

**نسخه:** ۱.۰.۰
**پلتفرم هدف:** Android و Windows (با قالب‌های iOS/MacCatalyst/Tizen)
**توسعه‌دهنده:** مصطفی یزدانی

---

## ۱) معرفی پروژه

FileTransferApp یک اپلیکیشن انتقال فایل و پیامرسانی محلی (همتا به همتا / P2P) است که روی شبکه محلی (LAN/Wi-Fi) بدون نیاز به سرور واسط کار میکند. دستگاهها بهصورت خودکار یکدیگر را کشف میکنند و میتوانند متن، فایل و تصویر مبادله کنند.

تمرکز مرحله پنجم (Stage 5) بر **امنیت کانال ارتباطی** است: احراز هویت دستگاهها، رمزنگاری سرتاسری و اعتماد مبتنی بر اثر انگشت کلید عمومی، بههمراه جفتسازی دستی با کد تأیید کوتاه (SAS).

## ۲) اهداف و قابلیتها

- کشف خودکار دستگاههای همشبکه
- ارسال متن، فایل و تصویر با پیشنمایش و نوار پیشرفت
- اعلان وضعیت («در حال نوشتن...») و ذخیره فایلهای دریافتی
- برقراری لینک دانلود نسخه ویندوز از طریق مرورگر (LAN Server)
- **Stage 5:** هویت ماندگار مبتنی بر امضای دیجیتال ECDSA
- **Stage 5:** کانال رمزنگاریشده AES-256-GCM با احراز هویت متقابل
- **Stage 5:** جفتسازی امن با کد ۶ رقمی SAS و مدیریت اعتماد (همیشه/یکبار/لغو)
- سیاست امنیتی پیشفرض **فقط-امن** (Enforce) با امکان اختیاری AllowLegacy

## ۳) معماری کلی و لایهبندی

| لایه | مؤلفهها |
|---|---|
| رابط کاربری | MainPage، ChatPage، AboutPage + ViewModelها (MainViewModel، ChatPageViewModel) |
| سرویسها | DeviceDiscoveryService، Message_Service، DownloadServerService، TrustService، DeviceIdentity، PreviewService، NotificationService |
| امنیت | Crypto، P2pChannel، SecureFrameStream، PairingService، TrustStoreModel |
| محدودیت/اعتبارسنجی | FileSizeValidator، DiskSpaceValidator، IncomingTransferGuard، TransferLimits، TransferTimeout |

معماری از اصل جداسازی لایهها پیروی میکند: کلاسهای امنیتی (`Crypto`، `P2pChannel`، `PairingService`، `TrustStoreModel`) فاقد وابستگی به MAUI هستند و به همین دلیل مستقیماً در پروژه تست واحد استفاده میشوند.

## ۴) کشف دستگاهها (Discovery)

`DeviceDiscoveryService` از **UDP broadcast** استفاده میکند:

- هر دستگاه بهصورت دورهای (هر ~۵ ثانیه) پیام حضور خود شامل نام، دستگاه و DeviceId را broadcast میکند.
- پیامهای دریافتی فیلتر میشوند (حذف خود دستگاه، IP پنهانشده).
- دستگاهها بهصورت گروهبندیشده بر اساس IP نمایش داده میشوند و آفلاین/آنلاین بودن از روی زمان آخرین حضور (آستانه ۶۰ ثانیه) تشخیص داده میشود.

## ۵) لایه انتقال و کانال ارتباطی

- **پورت TCP ۴۰۴۰** برای همه ترافیک پیامرسانی (شنونده واحد).
- هر اتصال جدید ابتدا اولین رشته را میخواند تا بین پروتکل امن (HELLO) و پروتکل قدیمی (TEXT/...) تمایز قائل شود.
- بافرهای socket و chunk اندازهگیری شده برای انتقال فایل (۲۵۶KB).
- سرور دانلود LAN روی **پورت ۸۰۸۰** با ریدایرکت به آخرین نصاب ویندوز (دریافت از اینترنت از طریق `RemoteConfigService`).

## ۶) پروتکل پیامرسانی

### ۶.۱) پروتکل امن (Stage 5)

| دستور | جهت | توضیح |
|---|---|---|
| `HELLO` | Client→Server | شروع دستدهی (حاوی sessionId، کلید عمومی بلندمدت، کلید موقت ECDHE و امضا) |
| `HELLO_ACK` | Server→Client | پاسخ دستدهی |
| `PAIR1` | Initiator→Responder | درخواست جفتسازی |
| `PAIR2` | Initiator→Responder | ارسال کد SAS برای مقایسه |
| `PAIR_ACCEPT` / `PAIR_ABORT` | هر دو جهت | نتیجه جفتسازی |
| `STATUS` | هر دو جهت | اعلان وضعیت |
| `TEXT2` | هر دو جهت | پیام متنی (با DeviceId) |
| `FILE_OFFER` | هر دو جهت | پیشنهاد فایل + اعتبارسنجی و پذیرش |

همه دستورهای امن از طریق قابهای رمزنگاریشده (`SecureFrameStream`) ارسال میشوند و دسترسی فقط پس از **تأیید اعتماد** (اثر انگشت) انجام میشود؛ در غیر این صورت با `NACK|Untrusted` رد میشوند.

### ۶.۲) پروتکل قدیمی (سازگاری)

دستورهای `TEXT`، `TEXT2`، `FILE_OFFER`، `FILE` و `STATUS` بدون دستدهی فقط در حالت صریح `AllowLegacy` پذیرفته میشوند. سیاست پیشفرض `Enforce` است و **هیچ fallback خودکار به پروتکل قدیمی وجود ندارد.**

## ۷) امنیت: هویت دستگاه و مدیریت اعتماد

- **هویت بلندمدت:** یک جفت کلید ECDSA روی منحنی P-256 که در Preferences ذخیره میشود (`DeviceIdentity`). کلید عمومی (SPKI) هویت دستگاه است و اثر انگشت آن یعنی `SHA-256(SPKI)` مرجع اعتماد است.
- **DeviceId** فقط یک برچسب انسانی است؛ اعتماد به پروتکلهای امن بر پایه اثر انگشت است نه DeviceId.
- `TrustStoreModel` وضعیتهای اعتماد را نگهداری میکند: `TrustedAlways`، `TrustedOnce`، `Revoked`، `KeyReplaced` (تشخیص تعویض کلید)، همراه با مهاجرت خودکار از ذخیرهگاه قدیمی.
- APIهای `TrustService` هم بهصورت کلیددار (deviceId + fingerprint) و هم نسخه قدیمی (فقط deviceId) موجودند؛ نسخه قدیمی هرگز به همتایان امن اعتماد نمیدهد.

## ۸) امنیت: دستدهی احراز هویت (Handshake)

پروتکل دستدهی در `P2pChannel`:

1. کلاینت یک `sessionId` تصادفی و جفت کلید موقت ECDHE میسازد.
2. **Transcript اولیه** (domain + نسخه + deviceId/نام/کلید بلندمدت/کلید موقت) ساخته و با کلید خصوصی ECDSA امضا میشود → `HELLO`.
3. سرور امضا را با کلید عمومی بلندمدت کلاینت **تأیید** میکند، جفت ECDHE خود را میسازد و **Transcript کامل** را امضا میکند → `HELLO_ACK`.
4. هر دو طرف با بررسی ثابتزمانی، یکسان بودن sessionId و صحت امضاها را کنترل میکنند.
5. **کلیدها:** توافق ECDHE → HKDF-SHA256 (پیادهسازی دستی RFC 5869) → کلید master + کلیدهای جهتدار C2S و S2C، همه گرهخورده به هش transcript.

این ساختار تضمین میکند که هر MITM که نتواند امضای معتبر بدهد، توسط مقایسه SAS هم شناسایی میشود.

## ۹) امنیت: رمزنگاری کانال (AES-256-GCM)

`SecureFrameStream` یک لایه Stream است که:

- هر فریم را با **AES-256-GCM** رمز میکند (nonce ۱۲ بایتی تصادفی، tag ۱۶ بایتی).
- هدر ۴۱ بایتی: magic + نسخه + sessionId + شماره توالی + nonce + طول.
- **AAD** شامل magic/نسخه/sessionId/شماره توالی است و **شمارنده توالی سختگیرانه** هر فریم را احراز میکند (محافظت در برابر replay؛ ترتیب را TCP تضمین میکند).
- حداکثر بار فریم ۲MB و chunk پیشفرض ۲۵۶KB.
- هر خرابی tag/توالی باعث ابطال اتصال میشود (fail-closed).

## ۱۰) پروتکل جفتسازی با کد SAS

جفتسازی با دکمه **Pair** در صفحه اصلی شروع میشود:

1. Initiator اتصال امن برقرار و `PAIR1` میفرستد.
2. هر دو طرف SAS را بهصورت مستقل محاسبه میکنند: `HMAC-SHA256(masterKey, domain ‖ transcriptHash) mod 1,000,000` → یک عدد ۶ رقمی.
3. در هر دو دستگاه کادر تأیید با همان عدد ۶ رقمی نمایش داده میشود (`PairingStarted` در سمت initiator و `PairingRequested` در سمت responder).
4. کاربر پس از مقایسه چشمی، تأیید را ثبت میکند (`CompletePairing`).
5. Initiator `PAIR2` (شامل SAS) میفرستد؛ responder SAS دریافتی را با SAS خودش مقایسه و در صورت تطابق `PAIR_ACCEPT` و در غیر این صورت `PAIR_ABORT` میفرستد.
6. در صورت موفقیت، هر دو طرف `TrustAlways` را روی اثر انگشت طرف مقابل ثبت میکنند.
7. مهلتهای زمانی: دستدهی ۱۰ ثانیه، کل فرایند جفتسازی ۱۸۰ ثانیه، کادر تأیید ۱۲۰ ثانیه.

نکته امنیتی: SAS هرگز روی شبکه ارسال نمیشود؛ فقط در `PAIR2` برای مقایسه فرستاده میشود و حمله MITM اعداد متفاوتی تولید میکند که مقایسه انسانی آن را کشف میکند.

## ۱۱) ساختار کد و فایلها

### سرویسها (`Services/`)

| فایل | سطر | نقش |
|---|---|---|
| MessageService.cs | ~۱۱۱۸ | پروتکل پیامرسانی، شنونده TCP، پذیرش فایل، جفتسازی |
| Crypto.cs | ~۲۱۶ | رمزنگاری: ECDSA، ECDHE، HKDF، AES-GCM، SAS |
| P2pChannel.cs | ~۵۰۶ | دستدهی و قابهای امن + SecureFrameStream |
| TrustStoreModel.cs | ~۲۲۶ | مدل ذخیره اعتماد |
| TrustService.cs | ~۱۷۸ | مدیریت اعتماد |
| PairingService.cs | ~۱۱۱ | منطق جفتسازی و رجیستری انتظار |
| DeviceIdentity.cs | ~۹۵ | هویت بلندمدت (کلید ECDSA) |
| DeviceDiscoveryService.cs | ~۲۵۶ | کشف دستگاهها (UDP) |
| DownloadServerService.cs | ~۲۶۸ | سرور دانلود LAN |
| TransferLimits.cs / TransferTimeout.cs | ~۵۳ / ~۳۹ | محدودیتها و زمانبندی |

### رابط کاربری

| فایل | نقش |
|---|---|
| MainPage.xaml (+ .cs) | صفحه اصلی، لیست دستگاهها، دکمه Connect/Pair، اوورلیهای LAN/About |
| ChatPage.xaml (+ .cs) | چت حبابی (الهام از تلگرام)، ارسال فایل/تصویر، نوار پیشرفت |
| ChatPageViewModel.cs | ~۹۱۲ | منطق چت، اعتماد، ارسال/دریافت |

## ۱۲) آزمونها (واحد و یکپارچه)

۹۰ تست در `FileTransferApp.Tests` — **همه سبز (Passed: 90/90)**

| کلاس تست | تعداد | پوشش |
|---|---|---|
| CryptoTests | ۲۱ | HKDF، ECDSA، ECDHE، AES-GCM، SAS، اثر انگشت |
| P2pChannelTests | ۱۱ | دستدهی دوطرفه روی socket، قابهای امن، replay، tamper، جداسازی session |
| TrustStoreModelTests | ۱۱ | اعتماد، لغو، تعویض کلید، مهاجرت، سریالسازی |
| PairingServiceTests | ۱۱ | کدک پیام، رجیستری، انقضای انتظار |
| FileSizeValidatorTests | ۱۳ | اعتبارسنجی نام/اندازه فایل |
| DiskSpaceValidatorTests | ۹ | فضای خالی دیسک |
| TransferTimeoutTests | ۹ | مهلتهای زمانی |
| IncomingTransferLimitTests | ۵ | محدودیت همزمانی ورودی |

تستهای شبکه، با `LoopbackPair` روی `TcpClient` واقعی اجرا میشوند و شامل تست بار یک میلیون بایتی، تغییر داده (tamper)، تکرار فریم (replay) و عدم تطابق sessionId هستند.

## ۱۳) ساخت، اجرا و محیطها

| هدف | دستور | نتیجه |
|---|---|---|
| Windows (WinUI) | `dotnet build -f net8.0-windows10.0.19041.0 -c Debug` | ✅ موفق، ۰ خطا |
| Android | `dotnet build -f net8.0-android -c Debug` | ✅ موفق، ۰ خطا |
| تستها | `dotnet test` روی FileTransferApp.Tests | ✅ ۹۰/۹۰ |

هشدارهای موجود از نوع XamlC XC0022 (بدون x:DataType) و CS8618 مربوط به کدهای پیشین هستند و بلوککننده نیستند.

## ۱۴) محدودیتها و گامهای بعدی

### محدودیتهای شناختهشده

- **کلید خصوصی در Preferences ذخیره میشود** (خصوصیِ اپ اما بدون پشتیبانی سختافزاری). انتقال به Android Keystore / امنسازی ویندوز به مرحله بعد موکول شده است.
- `SecureFrameStream` با غیرممکنبودن Read/Write همزمان در یک رشته (SemaphoreSlim) محدود است؛ برای این پروژه کافی است.
- پیامهای دریافتی از همتایان امن، تنها پس از جفتسازی (اعتماد مبتنی بر اثر انگشت) پردازش میشوند؛ کادر «اعتماد» چت عمدتاً برای حالت AllowLegacy است.
- هشدارهای کامپایل XamlC XC0022 باقی است (بهبود اختیاری).

### گامهای بعدی

1. نگهداری کلید در **Android Keystore / Secure Storage**.
2. بازنشانی اعتماد (Revoke) و نمایش اثر انگشت در رابط کاربری چت.
3. گزارش کامل هندشیک (نسخه، الگوریتمها) برای مقیاسپذیری پروتکل.
4. تستهای خودکار روی دستگاه واقعی Android (دو گوشی در یک شبکه).
5. انتشار نسخه ویندوز از طریق LAN Server با آدرس مستقیم.
